pipeline {
    agent any
    stages('Build') {
        stage {
            steps {
                echo 'Installing depenencies .....'
                sh 'npm install'
            }
        }
    }
}
